# CLAUDE.md — הנחיות לעבודה על הפרויקט

## רב-לשוניות (i18n) — עברית / אנגלית

המערכת תומכת בעברית (RTL) ובאנגלית (LTR). כל התשתית ב-`js/i18n.js` (נטען **ראשון** ב-`<head>`).

- **שפה נשמרת ב-localStorage** במפתח `app_lang` (`'he'` ברירת מחדל). מתג עב/EN ב-**3 מקומות**: navbar (דסקטופ), מסכי ההתחברות, ו**מגירת המובייל** (`.sidebar-lang-switcher`) — כי ה-navbar מוסתר מתחת ל-769px. כל מתגי `.lang-btn` נקלטים אוטומטית ב-`initLangSwitcher` ומסונכרנים.
- **כיוון נגזר מהשפה**: עברית=`rtl`, אנגלית=`ltr`. `body { direction: rtl }` נעקף ע"י `[dir="ltr"] body { direction: ltr }`, ויש בלוק עקיפות `[dir="ltr"]` ב-`css/styles.css` (sidebar, margins, יישור טקסט, כפתור עין).
- **⚠️ עקיפות layout של LTR (sidebar side + `main-content` margin) עטופות ב-`@media (min-width: 769px)`** — במובייל ה-sidebar הוא מגירה והתוכן מלא-רוחב, אז אסור ל-`margin-left` של LTR לחול (אחרת התוכן גולש). כלל mobile ו-LTR באותה specificity — סדר המקור קובע.
- **החלפת שפה** מפעילה `applyLang` → מרעננת תרגומים סטטיים, בונה מחדש `ROLE_LABELS`/`STATUS_LABELS` (`refreshI18nLabels` ב-`config.js`), משדרת `languagechange:app`, ומרנדרת מחדש את התצוגה הפעילה דרך `window.rerenderCurrentView` (ב-`app.js`) — בלי reload, תוך שמירת ההקשר (פרויקט/פוד).

### כללים בעת הוספת/שינוי טקסט
1. **אין לקודד טקסט קשיח.** כל מחרוזת מוצגת עוברת דרך `t('namespace.key')`. מוסיפים מפתח **גם ל-`he` וגם ל-`en`** ב-`js/i18n.js`.
2. **HTML סטטי ב-`index.html`**: משתמשים ב-`data-i18n`, `data-i18n-placeholder`, `data-i18n-title`, `data-i18n-aria-label`.
3. **פרמטרים**: `t('key', { name })` מחליף `{name}` במחרוזת.
4. **⚠️ התנגשות שם `t`**: בקוד יש לולאות רבות עם משתנה `t` (טיפוס) שמסתיר את פונקציית התרגום `t()`. בתוך `.map(t => ...)`/`forEach(t => ...)` **יש לשנות את משתנה הלולאה ל-`ty`** אם קוראים ל-`t()` בפנים.
5. **תוכן QC**: מתורגם דרך `qcStageName` / `qcItemLabel` / `qcItemInstruction` / `qcItemUnit` ב-`qc-data.js` (שדות `labelEn`/`instructionEn`/`nameEn`/`unitEn` לצד העברית).

### מה נשאר בעברית בכוונה (קנוני — לא לתרגם)
- **כתיבות ל-DB** (`stage_name`, `item_label`) — עברית קנונית; התצוגה נגזרת מחדש לפי `item_key`/`stage_number`.
- **`GROUP_NAME_OPTIONS`** ב-`projects.js` — משמש להתאמה/מיון של שמות קבוצות קיימים.
- הערות קוד.

**בעת החלפת גרסת קובץ**: יש לעדכן את מספר ה-`?v=` ב-`index.html`.

## מודל אבטחה (RLS) — מאז 20260712_critical_security_fixes

המיגרציה `supabase/migrations/20260712000000_critical_security_fixes.sql` מגדירה את מודל ההרשאות. **מיגרציות חייבות להיות מוחלות ידנית ב-Supabase SQL Editor** — פריסת האתר לא מחילה אותן.

- **תפקיד משתמש חדש נקבע רק בשרת** (`handle_new_user` → תמיד `viewer`). אין לשלוח `role` ב-metadata של `signUp`.
- **משתמש חדש נוצר `is_active=false`** וממתין לאישור אדמין במסך ניהול המשתמשים. עד אז הוא לא רואה נתונים (RLS) ומקבל את מסך `auth.pendingApproval` (`showPendingApprovalScreen` ב-`app.js`).
- **כל הפוליסות משתמשות ב-`public.current_user_role()`** — פונקציית SECURITY DEFINER שמחזירה את התפקיד רק אם `is_active=true` (אחרת NULL). נטרול משתמש חוסם אותו בפועל. פוליסות חדשות חייבות להשתמש בפונקציה הזו, לא ב-`EXISTS (SELECT ... FROM profiles)` (גורם לרקורסיה בפוליסות של profiles עצמה).
- **שינוי `role`/`is_active`/`email`/`username` בפרופיל** — רק אדמין פעיל (trigger `protect_profile_privileges`).
- **כתיבה ל-QC (`qc_stages`/`qc_items` + תמונות `qc-images`)**: admin + project_manager + inspector (מנהל פרויקט נוסף במיגרציה 20260719010000; בצד לקוח — `canEdit()`). **בדיקות תבניות (`mold_checks`) נשארו admin + inspector בלבד** — בצד לקוח `canEditMolds()`. **עדכון `pods`** (status/casting) כולל גם inspector. **שם התפקיד בפוליסות הוא `'project_manager'`** — לא `'pm'` (באג היסטורי בפוליסות התוכניות).
- **Buckets פרטיים (מאז 20260712010000)**: `plans` ו-`qc-images` אינם ציבוריים. תצוגה נעשית עם **Signed URLs** (שעה): `signQcImageUrls` ב-`qc.js` (מחליף `image_url` בזיכרון) ו-`signedByPath` ב-`loadPlansTab` ב-`projects.js`. `file_url`/`image_url` ב-DB הם fallback לרשומות ישנות — אין להסתמך עליהם לתצוגה.
- **בדיקת שם משתמש תפוס בהרשמה**: דרך `username_exists` (boolean). `get_email_by_username` נשארת רק להתחברות עם שם משתמש.
- **מחיקת קבוצת ביצוע** מנתקת פודים (`pods.group_id` = `ON DELETE SET NULL`, מיגרציה 20260712020000) — לא מוחקת אותם.
- **SRI ב-`index.html`**: כל ספריות ה-CDN מוצמדות לגרסה מדויקת עם `integrity`. בעת שדרוג ספרייה יש לחשב sha384 חדש מקובץ ה-dist שב-tarball של npm (`openssl dgst -sha384 -binary | openssl base64 -A`).

## סוג מוצר בפרויקט — פודים סניטריים מול פאנלים רפואיים

מאז מיגרציה `20260803000000_project_product_type.sql` לכל פרויקט יש `projects.product_type`:
`'pod'` (ברירת מחדל — כל הפרויקטים הקיימים) או `'medical_panel'` (ראש מיטה מודולרי לבתי חולים).
נבחר **בעת יצירת הפרויקט בלבד** (`np-product-type` ב-`buildNewProjectForm`) ואינו ניתן לשינוי אחר כך.

⚠️ **המיגרציה חייבת לרוץ ידנית ב-SQL Editor לפני יצירת פרויקט פאנלים.** `createProject` שולח
`product_type` **רק כשהוא שונה מ-`'pod'`**, ולכן יצירת פרויקטי פודים ממשיכה לעבוד גם בלי המיגרציה;
יצירת פרויקט פאנלים תיכשל עם שגיאה במקום ליצור פרויקט שגוי בשקט.

⚠️ **שאילתות ששולפות את הפרויקט המקושר משתמשות ב-`projects(*)` ולא ברשימת עמודות מפורשת**
(`openPod` ב-`pods.js`, `loadReportsView` ב-`reports.js`). ברשימה מפורשת, `product_type` שאינו
קיים ב-DB היה מפיל את כל השאילתה ושובר את מסך הפוד לפני הרצת המיגרציה. אין להחזיר רשימת עמודות מפורשת.

### שני סטים של שלבי QC

| | פוד סניטרי | פאנל רפואי |
|---|---|---|
| קבוע | `QC_STAGES` (6 שלבים A–F) | `PANEL_STAGES` (5 שלבים A–E) |
| נעילה | שער יציקה — B–F נעולים עד אישור סעיפי היציקה | **נעילה עוקבת** — שלב נפתח רק אחרי חתימת קודמו |
| קוד יחידה | `ABC-DDMMYY-T1-R-001` | `ABC-DDMMYY-T1-001` (ללא R/L) |

- **`qcStageSet(productType)`** ב-`qc-data.js` מחזיר את הסט הנכון; **`podProductType(pod)`** קורא
  את הסוג מ-`pod.projects.product_type` (ברירת מחדל `'pod'`).
- **`getStage(num, productType)` ו-`qcStageName(num, productType)` מקבלים סוג מוצר.** הפרמטר
  אופציונלי ומשמעו `'pod'` — כך קריאות ישנות ממשיכות לעבוד. **בכל קריאה חדשה חובה להעביר את הסוג**,
  אחרת סעיפי פאנל ייפתרו מול הגדרות הפודים ויוצגו שמות שגויים.
- **אין להשתמש ב-`QC_STAGES` ישירות בקוד תצוגה/דוחות** — רק דרך `qcStageSet`. הקבוע `6` כמספר
  שלבים הוסר מכל חישובי האחוזים; המכנה נגזר מ-`qcStageSet(...).length`.
- **`_qcProductType`** ב-`qc.js` נקבע ב-`loadQCStages` מהפוד הפעיל ומשמש את כל הפונקציות במודול.
  `_qcIsPanel()` שומר על כל נתיבי היציקה (אישור, ביטול, ניקוי שלב A, באנר) מלרוץ על פאנלים.

### נעילה עוקבת (`panelStageLocked`)
`panelStageLocked(stageIdx)` מחזיר `true` אם **אחד** השלבים הקודמים אינו `completed`/`failed`.
התוצאה נכנסת ל-`readonly` (הסעיפים לא ניתנים לסימון), מציגה באנר `qc.seqLockedBanner`, ומסתירה את
אזור החתימה — כך אי אפשר לדלג על שלב. **הפונקציה מחזירה `false` מיד עבור פודים** ולכן שער היציקה
לא מושפע.

### מה מוסתר בפרויקט פאנלים
קבוצות ביצוע, בדיקות תבניות (שני הטאבים מוסתרים ב-`openProject` ולא נטענים), כיוון ימין/שמאל,
סוג צנרת, אישור יציקה, ומסנני קבוצה/כיוון/יציקה (`setupPodFilters` גם **מאפס** אותם — מסנן ישן
שנשאר דרוך היה מסתיר את כל הפאנלים).

### מידות הפאנל — רוחב וגובה בלבד
לפאנל אין אורך. בפרויקט פאנלים שדה "אורך" **אינו מוצג** — לא במודל היצירה (`renderTypeInputs`)
ולא בטאב "פרטים נוספים" (`loadProjectDetailsTab`). כתוצאה מכך `dimensions` נשמר בפורמט **`WxH`**
(שני מקטעים) ולא `LxWxH`. ⚠️ **`parseDims` חייבת לקרוא את הפאנלים כ-`w`,`h`** — קריאה כ-`l`,`w`
הייתה מציגה את הרוחב בשדה האורך ואת הגובה בשדה הרוחב.

### מבנה היחידות
שלושה דגמים בפרויקט בילינסון: T1 = A100 בסיסי (244 ס"מ), T2 = A100 מקוצר (220 ס"מ), T3 = B100 מוגבר.
**אין ימין/שמאל** — נוצרת שורת `type_directions` יחידה עם `direction='R'` כ-placeholder כדי לא לשנות
את סכמת `pods.direction_id`; היא לעולם אינה מוצגת, ו-`generatePodCode` מקבל `direction=''` ומשמיט
את המקטע. **הסיריאל נשאר 3 התווים האחרונים בקוד**, ולכן כל המיונים לפי סיריאל עובדים ללא שינוי.

⚠️ **כמויות וגבהים אינם מקודדים ב-`PANEL_STAGES`.** אותו אביזר יושב בגבהים שונים בפאנלים שונים,
ותכולת הקופסאות שונה בין דגמים — לכן הוראות הבדיקה מפנות לתוכנית של הפאנל הספציפי ומבקשות לרשום
את הערך שנמדד בהערה. אין להחליף את זה במספר קבוע.

## שם דגם לטיפוס (`project_types.model_name`)

מאז מיגרציה `20260803020000_type_model_name.sql`. שדה טקסט חופשי ואופציונלי.

**הרקע**: הבודק רואה `T1` ואין לו דרך לדעת איזה דגם זה. המידות לא פותרות — בפרויקט בילינסון
ל-T1 (בסיסי) ול-T3 (מוגבר) יש **בדיוק אותן מידות** `120x244`, כך שרק שם מפורש מבדיל ביניהם.

- **`typeLabel(ty)`** ב-`config.js` הוא נקודת התצוגה היחידה: מחזיר `T1 — בסיסי`, או `T1` בלבד
  כששם הדגם ריק. **אין לכתוב `T${ty.type_number}` ישירות בקוד תצוגה חדש** — כך שם הדגם מופיע
  אוטומטית בכל מקום, ופרויקטים ישנים (ללא שם) נראים בדיוק כמו קודם.
- מוצג ב: כרטיס הפוד/פאנל, מסך הפוד, מסנן הטיפוסים, טאב התוכניות, טאב הפרטים, טבלת הדוחות,
  ייצוא האקסל וכותרת ה-PDF.
- ⚠️ **לא מוצג בברקוד — בכוונה.** נוסה והוסר: על המדבקה מספיק קוד היחידה. תצוגת הברקוד
  משותפת לפודים ולפאנלים (`showBarcodeModal` + `printAllBarcodes`), ומשבצת ה-`group-marker`
  מציגה שם קבוצה כשיש ואחרת נשארת ריקה. אין להוסיף שם דגם למדבקה.
- **ניתן לעריכה בדיעבד** בטאב "פרטים נוספים" (`.det-model-name`), ולא רק ביצירת הפרויקט —
  פרויקטים שכבר נוצרו צריכים דרך לקבל שמות.
- שאילתות `project_types` עברו ל-`select('*')` / `project_types(*)` מאותה סיבה שמתוארת
  בסעיף סוג המוצר: רשימת עמודות מפורשת נשברת לפני הרצת המיגרציה.
  ⚠️ **גם `loadProjectDetailsTab` חייבת `select('*')`** — היא נשכחה בהתחלה (רשימה מפורשת בכמה
  שורות), והתוצאה הייתה שדה "שם דגם" שנראה כאילו אינו נשמר: הערך נכתב ל-DB כרגיל, אבל השאילתה
  לא החזירה את העמודה ולכן השדה נטען ריק בכל כניסה מחדש. בהוספת עמודה ל-`project_types` יש
  לוודא שכל השאילתות מחזירות אותה.

## פורמט קוד היחידה — מספר פרויקט במקום יום בחודש

מאז מיגרציה `20260803010000_project_number.sql` יש `projects.project_number` (INTEGER, nullable),
והמקטע האמצעי בקוד היחידה נגזר ממנו:

| | פרויקטים קיימים (`project_number IS NULL`) | פרויקטים חדשים (מספר מוגדר) |
|---|---|---|
| מקטע אמצעי | `DDMMYY` — תאריך קבלה מלא | `PPMMYY` — מספר פרויקט + חודש/שנה |
| דוגמה | `EKR-220226-T1-R-001` | `BLN-020726-T1-001` |

- **`generatePodCode(code, date, type, dir, serial, projectNumber)`** — הפרמטר השישי אופציונלי.
  ריק/NULL ⇒ פורמט ישן; מספר > 0 ⇒ פורמט חדש. **כל קריאה חדשה חייבת להעביר את המספר**, אחרת
  ייווצר קוד בפורמט הישן בשקט.
- ⚠️ **אין למלא `project_number` רטרואקטיבית לפרויקטים קיימים.** קודי הפודים כבר כתובים ב-
  `pods.pod_code` ואינם נגזרים מחדש, כך שמילוי העמודה לא ישנה אותם — אבל *פודים חדשים* שיתווספו
  לאותו פרויקט יקבלו פתאום פורמט אחר מאחיהם. EKR הוא פרויקט מספר 1 ונשאר בלי מספר.
- **המספר נבחר ביצירת הפרויקט** (`np-project-number`), עם ברירת מחדל `max(project_number)+1`
  (fallback 2, כי EKR=1 אינו מסומן). ניתן לעריכה ידנית.
- **`createProject` בודק מראש שהעמודה קיימת** (`select('project_number').limit(1)`) ונעצר עם
  `proj.errNeedNumberMigration` אם לא. הבדיקה קיימת כדי שלא ייווצרו קודים בפורמט הישן בשקט —
  אין להסיר אותה לטובת "ניסיון ואז fallback".
- **`showGroupModal` שולף את הפרויקט ב-`select('*')`** ולא ברשימת עמודות מפורשת, מאותה סיבה
  שמתוארת בסעיף סוג המוצר.
- הסיריאל נשאר 3 התווים האחרונים בשני הפורמטים, ולכן כל המיונים לפי סיריאל עובדים ללא שינוי.

## יעדי משלוח ותעודת משלוח (`pod_destinations`)

מאז מיגרציה `20260824000000_pod_destinations.sql` (**חובה להריץ ידנית ב-SQL Editor**).
נבנה לפרויקט המעונות: 5 בניינים A–E, 6 קומות (0 = קרקע, 1–5), וקודי חדרים שמסופקים ע"י המזמין.

### המודל
טבלת יעדים לפרויקט + מצביע מהפוד ליעד — **לא** שלושה שדות טקסט על הפוד:

| | |
|---|---|
| `pod_destinations` | `project_id`, `building`, `floor`, `room_code`, `type_number`, `direction`, `notes` |
| ייחודיות | `UNIQUE (project_id, building, floor, room_code)` — אין שני חדרים זהים |
| `pods.destination_id` | FK עם `ON DELETE SET NULL` — מחיקת יעד מנתקת את הפוד ולא מוחקת אותו |
| חדר = פוד אחד | אינדקס ייחודי חלקי `uniq_pods_destination` (רק על ערכים לא-NULL) |
| הרשאות | צפייה לכל משתמש פעיל; ניהול ל-admin + project_manager (כמו `production_groups`) |

- **`floor` הוא INTEGER ו-0 הוא קומת הקרקע** — כדי שהמיון יהיה נכון (טקסט היה ממיין '10' לפני '2').
  התצוגה נגזרת ב-`destFloorLabel`; **אין לשמור "קרקע" כטקסט**.
- **`type_number`/`direction` ביעד הם ציפייה בלבד** (משמשים את השיוך האוטומטי), לא אילוץ —
  אפשר לשייך ידנית כל יחידה לכל חדר.

### הזרימה (טאב "יעדי משלוח" ב-`js/destinations.js`)
1. **ייבוא** רשימת החדרים מהמזמין — קובץ אקסל/CSV או הדבקה ישירה מ-Excel (שניהם עוברים דרך
   `parseDestImport`). עמודות: בניין | קומה | קוד חדר | טיפוס | כיוון. שורת כותרת מזוהה ומדולגת,
   כפילויות ושורות שגויות מדווחות בתצוגה מקדימה לפני הכתיבה.
2. **שיוך אוטומטי** (`autoAssignDestinations`) — עובר על החדרים בסדר האתר ומשייך לכל אחד את
   היחידה הפנויה הראשונה בסדר הסיריאל שמתאימה לטיפוס (ולכיוון, בפודים). **חדר בלי טיפוס מתוכנן
   נשאר לשיוך ידני** — ניחוש שם היה שולח יחידה לחדר הלא נכון.
3. **תיקון ידני** — כפתור "שייך" פותח בורר עם חיפוש, שמציג רק יחידות פנויות ומקדים את המתאימות.
4. **תעודת משלוח** — כפתור 🚚 במסך הפוד (`generateDeliveryNote`).

⚠️ **`parseDestImport` לא מבצע `trim` על השורה כולה לפני הפיצול** — שורה שהתא הראשון בה ריק
הייתה מאבדת את המפריד המוביל וכל העמודות היו נדחפות אחת שמאלה (קוד החדר היה נכנס לעמודת הקומה).
מבצעים trim לתאים בלבד. באותה רוח **`parseDestFloor` דוחה כל דבר שאינו מספר** (`/^\d{1,2}$/` או
מילת קרקע) ולא "מחלץ" ספרות — `D-300` בעמודת הקומה חייב להפיל את השורה ולא להתפרש כקומה 300-.

### תעודת המשלוח
- **צינור ה-PDF משותף עם דוח ה-QC**: `renderSectionsToPdf(sections, filename, opts)` + `pdfLogoDataUrl` +
  `pdfBarcodeDataUrl` ב-`reports.js` (חולצו מ-`buildAndDownloadPDF`). מתמטיקת העמודים ושמירת
  הקובץ קיימות **במקום אחד בלבד** — אין לשכפל אותן.
- **הפקה לפי סינון**: כפתור `rf-btn-delivery` בטאב "דוח QC" שבמסך הדוחות מפיק
  `generateDeliveryNotesForPods(filtered, btn)` — **קובץ אחד, תעודה בכל עמוד**, בסדר שמוצג בטבלה.
  משלוח נמסר כחבילה אחת, לא כ-30 הורדות נפרדות. הכפתור משתמש ב-`pod.deliveryNote` — **אותו מפתח
  תרגום** של הכפתור במסך הפוד, כדי שהתווית לא תתפצל לשתי מחרוזות.
- ⚠️ **שלוש אופציות ב-`renderSectionsToPdf` נועדו למנת האצווה** ואין לבטלן:
  `pageBreakEach` (אחרת שתי תעודות נדחסות לעמוד אחד), `scale: 2` + `jpegQuality: 0.85`
  (PNG@3x של דוח ה-QC היה מייצר מאות MB ל-200 תעודות), ו-`onProgress` למונה על הכפתור.
  בנוסף, הקנבסים **מרונדרים ומוזרקים אחד-אחד** ולא נצברים במערך — צבירה של 200 קנבסים
  מיצתה את הזיכרון. מדידה בפועל: ~0.3 שניות ו-~100KB לתעודה.
- מעל 40 תעודות מוצגת `uiConfirm` על משך ההפקה, ואם ליחידות שנבחרו אין כתובת — `uiConfirm`
  שמונה כמה מתוך כמה יופקו בלי כתובת.
- עמוד A4 אחד: כותרת + ברקוד, בלוק כתובת גדול (בניין / קומה / חדר), פרטי היחידה, ושורות חתימה
  של מוסר ומקבל. יחידה בלי יעד מפיקה תעודה רק אחרי `uiConfirm`, עם הבלטה שאין כתובת.
- **הכתובת נשלפת מחדש בעת ההפקה** ולא מהעותק בזיכרון, כדי שלא תודפס כתובת ישנה.

⚠️ **`openPod` שולף את היעד בשאילתה נפרדת ולא כ-embed** (`pod_destinations(...)`) — לפני הרצת
המיגרציה הטבלה וה-FK לא קיימים, ו-embed היה מפיל את כל שאילתת הפוד ושובר את מסך הפוד. בלי
העמודה `destination_id` פשוט undefined ולא מתבצעת שליפה. אותה סיבה שמתוארת בסעיף סוג המוצר.

## יעד ייצור וגריעת פודים (מיגרציה 20260824010000)

⚠️ **חובה להריץ ידנית ב-SQL Editor.** בלעדיה טבלת היעד לא מוצגת, ושמירת יעדים / פסילה נכשלות
עם הודעה מפורשת — שאר המערכת עובדת.

### כמות היעד (`type_directions.target_quantity`)
- נשמרת **לפי טיפוס+כיוון** על שורת `type_directions` (לפאנלים — שורת ה-placeholder היחידה של
  כל דגם, כך שאותו מודל משרת את שני סוגי המוצר). נערכת בטאב "פרטים נוספים" (`.det-target-qty`);
  שדה ריק = אין יעד למשבצת (מוצג `—`, לא 0).
- **טבלת "יעד ייצור"** (`renderTargetSummary` ב-`projects.js`, מכולה `#project-target-summary`
  מעל שורת המסננים): כמות יעד / הוזנו / נפסלו / נשארו לבצע, לכל משבצת + סה"כ. חריגה מעל היעד
  מוצגת `+N` באדום; עמידה ביעד `✓ 0` בירוק.
- ⚠️ **הטבלה תמונה פרויקטלית — מחושבת רק בטעינה לא-מסוננת.** `group_id` ו-`status` מיושמים
  בשאילתת הפודים, ולכן כשהם פעילים `allPods` כבר מצומצם; במקרה כזה `loadPodsTab` **לא** מרנדרת
  את הטבלה מחדש ומשאירה את הרינדור הקודם. אין לחשב אותה מ-`filtered`.
- **כפתור צמצום** (`btn-target-collapse`): מצב מצומצם מציג רק את הכותרת + שורת תקציר
  ("נשארו לבצע X מתוך Y"). **ברירת המחדל היא מצומצם** — הטבלה נפתחת רק בלחיצה, והבחירה
  נשמרת ב-`localStorage` (`qc_target_collapsed`, `'0'` = מורחב) ולכן שורדת רענונים, החלפת
  שפה ושינויי מסננים. שינוי ברירת המחדל נעשה בתנאי הקריאה של המפתח ב-`renderTargetSummary`.
- לפני המיגרציה המפתח `target_quantity` נעדר מהשורות ⇒ הטבלה פשוט לא מוצגת, ובטאב הפרטים
  מופיעה הודעת המיגרציה במקום השדות (`targetsAvailable` ב-`loadProjectDetailsTab`).

### גריעת פוד (`pods.is_scrapped` + `scrapped_reason`/`scrapped_at`)

**מינוח**: בעברית הפיצ'ר נקרא **"גריעה"** (סמן כנגרע / נגרעו / בטל גריעה) — הוחלף מ"פסילה"
לבקשת המשתמשת. שמות המפתחות, העמודות וה-actions ב-DB נשארו `scrap*` — שינוי תווית נעשה
ב-i18n בלבד, אין לגעת ב-`is_scrapped`/`pod_scrapped`.
- **הפוד לא נמחק ולא ממוחזר**: היסטוריית ה-QC נשמרת, הסיריאל שלו לא חוזר למאגר
  (`globalSerial` ממשיך כרגיל), ופוד חלופי מקבל סיריאל חדש. הגריעה רק מוציאה אותו מספירת
  "הוזנו" ומעבירה אותו לעמודת "נגרעו".
- **סימון**: כפתור במסך הפוד (`btn-pod-scrap`, admin+PM בלבד) — גריעה **מחייבת סיבה** (מודל),
  ביטול גריעה ב-`uiConfirm`. `refreshPodScrapUI` מעדכן תג ✖ בכותרת (הסיבה ב-tooltip) ואת
  הכפתור.
- **תיעוד**: trigger `log_pod_scrap_change` כותב `pod_scrapped`/`pod_unscrapped` ל-`qc_audit_log`
  עם הסיבה — מוצג בהיסטוריית הפוד (📜) דרך `audit.pod_scrapped`/`audit.pod_unscrapped`.
- **השפעות תצוגה**: כרטיס הפוד מקבל `pod-card-scrapped` (עמעום + קו חוצה) ותג ✖; פוד נגרע
  **מוחרג ממאגר השיוך של יעדי המשלוח** (`autoAssignDestinations` + הבורר הידני) ולא נספר
  כ"פוד ללא יעד". סטריפ הסטטוסים (ממתין/בתהליך/...) **לא** משתנה — טבלת היעד היא המקור
  לספירת הייצור.

## חשוב: לוגיקת מיספור פודים — אין לשנות

מיספור הפודים (`globalSerial`) רץ ברצף אחד רציף על פני **כל** הטיפוסים והכיוונים בפרויקט, לפי סדר הוספתם.

- כל קבוצת ביצוע (`production_group`) יכולה להכיל שלל סוגי פודים (T1-R, T1-L, T2-R וכו').
- המספרים משקפים את סדר הוספת הפודים לפרויקט (לפי סדר הקבוצות), לא את הטיפוס או הכיוון.
- כתוצאה מכך, בתצוגה מסוננת לפי טיפוס/כיוון ייתכנו "פערים" במספרים — זה מכוון ותקין.

**אין לשנות את `globalSerial` או את לוגיקת יצירת קודי הפודים ב-`createProject`.**

## מיון תצוגת פודים

הפודים ממוינים תמיד לפי **3 הספרות האחרונות** של קוד הפוד (המספר הסידורי) בסדר עולה.

- הלוגיקה נמצאת ב-`js/projects.js` בפונקציה `loadPodsTab`.
- אין למיין לפי טיפוס, כיוון, או השוואת מחרוזת מלאה — רק לפי הסיריאל.

```js
const getSerial = code => parseInt((code || '').slice(-3)) || 0;
const allPods = (pods || []).sort((a, b) => getSerial(a.pod_code) - getSerial(b.pod_code));
```

## סינון פודים לפי שלב (`filter-stage`)

מסנן שישי בטאב הפודים, לצד קבוצה/טיפוס/כיוון/סטטוס/יציקה. בוחרים שלב — ומוצגים רק הפודים
ש**השלב הנבחר אצלם בביצוע או הושלם**.

- **הגדרת ההתאמה**: `STAGE_FILTER_STATUSES = ['in_progress', 'completed']` ב-`js/projects.js`.
  שלב `pending` — או פוד שאין לו בכלל שורת `qc_stages` (פוד שאיש לא פתח) — נחשב "טרם הגיע לשלב"
  ולכן אינו מוצג. **`failed` אינו נכלל** בהתאם להגדרת הפיצ'ר; שינוי המדיניות נעשה בקבוע הזה בלבד.
- **הסינון בצד לקוח** על `pod.qc_stages(stage_number, status)` שכבר נשלף ב-`loadPodsTab`
  (`podStageStatus(pod, stageNumber)`) — אין שאילתה נוספת ואין שינוי בשאילתת הפודים.
- **רשימת השלבים נגזרת מ-`qcStageSet`** לפי סוג המוצר: A–F לפודים, A–E לפאנלים. בניית האופציות
  מחדש ב-`setupPodFilters` גם מאפסת בחירה ישנה, כך שמעבר בין פרויקטים (ובפרט בין פוד לפאנל)
  לא משאיר מסנן דרוך שמסתיר הכל.
- ⚠️ **המיון לא השתנה** — הפודים נשארים ממוינים לפי הסיריאל (ראו הסעיף הקודם). כשהמסנן פעיל
  מוצג על כרטיס הפוד תג נוסף (`.stage-filter-badge`, למשל "שלב B: בביצוע") שמסביר למה הוא ברשימה.
- **סטריפ הסטטיסטיקות וציר ההתקדמות מתעדכנים לפי הסינון** — ראו הסעיף הבא. **גם הדפסת הברקודים
  מושפעת** — `printAllBarcodes` קורא את הכרטיסים המוצגים בפועל, ולכן אפשר להדפיס ברקודים לפודים
  שנמצאים בשלב מסוים.
- מפתחות i18n: `filter.allStages`, `filter.stageOpt`, `proj.stageBadge`,
  `proj.noPodsInStage` / `proj.noPanelsInStage` (`he`+`en`).

### אותו מסנן במסך הדוחות (`rf-stage`)
טאב "דוח QC" מסנן באותו כלל בדיוק (`STAGE_FILTER_STATUSES` + `podStageStatus` מ-`projects.js`),
ולכן גם ייצוא ה-PDF, האקסל **ותעודות המשלוח** מכבדים אותו.

⚠️ **רשימת השלבים שם נבנית ב-`syncStageOptions` ותלויה בפרויקט הנבחר**, כי מסך הדוחות חוצה
פרויקטים: פרויקט בודד ⇒ שמות השלבים של סוג המוצר שלו; בחירה מעורבת (פודים + פאנלים) ⇒
**אותיות בלבד** (`filter.stageOptShort`), כי אותו מספר שלב הוא שלב אחר בכל סט. מאזין ה-`change`
של מסנן הפרויקט קורא ל-`syncStageOptions` **לפני** `renderTable`, ובחירת שלב שאינה קיימת בסט
החדש מתאפסת — אחרת היה נשאר מסנן דרוך שמסתיר הכל.

## מיון קבוצות ביצוע

קבוצות ממוינות תמיד לפי `sortGroupsByOption` ב-`js/projects.js`:
- קודם לפי `GROUP_NAME_OPTIONS` (שמות עבריים קבועים)
- fallback נומרי לשמות כגון G1, G2 ... G10 (מיון לפי המספר, לא אלפביתי)

אותו fallback קיים גם ב-`loadReportsView` ב-`js/reports.js`.

## מיון תצוגת הדוחות

- הדוחות מציגים רק פרויקטים ופודים **פעילים** (לא ארכיון). הסינון נעשה ב-`loadReportsView`:
  ```js
  const pods = (allPods || []).filter(p => p.projects?.is_active !== false);
  ```
- הפודים בטבלת הדוחות (טאב "דוח QC") ממוינים לפי 3 הספרות האחרונות של קוד הפוד.
- שני כפתורי הייצוא (PDF ו-Excel) בסגנון `btn-primary` זהה.

## מסך הדוחות — שלושה טאבים

`loadReportsView` שולפת **פעם אחת** את הפרויקטים והפודים ל-`_repProjects`/`_repPods` (module scope), ומרנדרת טאב לפי `_repTab`. שלושת הטאבים חולקים את אותם נתונים — אין שאילתת פודים נוספת בהחלפת טאב.

| טאב | פונקציה | מקור הנתונים |
|---|---|---|
| `qc` — דוח QC | `renderQcExportTab` (בתוך `loadReportsView`) | הפודים שנטענו; `qc_items` בעצלתיים בייצוא בלבד |
| `progress` — התקדמות עבודה | `renderProgressTab` | `qc_stages.completed_at` + `pods.inspection_started_at` (מהפודים שנטענו) + שאילתה ל-`qc_items` לפי `created_at` |
| `history` — היסטוריית עריכות | `renderHistoryTab` | `qc_audit_log` לפי `created_at` |

- **מצב נשמר ב-module scope** (`_repTab`, `_repRange`, `_repProject`) כדי שהחלפת שפה (`rerenderCurrentView`) ומעבר בין טאבים לא יאבדו את הבחירה.
- **טווח זמן** (`repRange`): פריסטים `24h`/`7d`/`30d` נמדדים אחורה מ-`now`; טווח מותאם נקרא כ-**חצות מקומי עד סוף היום המקומי**, כך שבחירת יום אחד מכסה אותו במלואו.
- **מדדי ההתקדמות**: `qc_stages.completed_at` (מועד חתימה מדויק) ו-`qc_items.created_at` (שורת סעיף נוצרת בסימון הראשון). השני הוא מה שחושף עבודה על שלב שמולא אך **טרם נחתם** — דוח שמבוסס על חתימות בלבד היה מציג פודים כאלה כרדומים. שני התאריכים נכתבים ב-DB ולכן מכסים את כל ההיסטוריה, בשונה מ-`qc_audit_log`.
- **מיון**: הטאבים החדשים ממוינים לפי **פעילות אחרונה בסדר יורד** — במכוון, ולא לפי סיריאל כמו טאב ה-QC. בדוח חלון-זמן מיון לפי סיריאל היה מקבור את עבודת היום.
- **תקרות שאילתה**: `qc_items` מוגבל ל-5000 שורות ו-`qc_audit_log` ל-500. בפגיעה בתקרה מוצגת הודעה (`rep.itemsTruncated` / `rep.historyTruncated`) — **אין להסתיר את זה**, אחרת הדוח מדווח מספרים חסרים בשקט.
- **סינון פרויקט בטאב ההיסטוריה** מפיל רשומות `mold_checks` (אין להן `pod_id` ולכן אי אפשר לשייך אותן לפרויקט). בתצוגת "כל הפרויקטים" הן מוצגות עם התווית `rep.moldCheckRow`.
- שני הטאבים החדשים גלויים לכל משתמש פעיל; `qc_items` ו-`qc_audit_log` פתוחים ל-SELECT לכל `current_user_role() IS NOT NULL`.

### ייצוא Excel בטאבים החדשים

`repExportExcel` + `repExportMeta` (ב-`reports.js`) — גיליון אחד לכל דוח, נפרד מ-`buildExcelFromPods` של טאב ה-QC.

- **הטבלה והקובץ נבנים מאותו מערך `flat`** — אין חישוב כפול, ולכן הקובץ לא יכול להתנתק ממה שמוצג על המסך. בהוספת עמודה יש לעדכן את `flat`, את ה-`<thead>`, את `headers` ואת `dataRows`.
- **בלוק כותרת חובה בקובץ**: טווח הזמן, סינון הפרויקט ומועד ההפקה. דוח חלון-זמן שנשלח במייל בלי הטווח שלו כתוב בתוכו הוא חסר משמעות.
- **אזהרת תקרה נכתבת גם לקובץ** (`⚠️` בבלוק הכותרת), לא רק למסך — אחרת קובץ עם 5000 סעיפים נראה מלא בעוד שהוא חסום.
- אחוז ההתקדמות נכתב כ**מספר** עם פורמט `0%` (לא מחרוזת "33%") כדי שיהיה מיון וסינון ב-Excel.
- `!views = [{ RTL: true }]` נוסף רק כשהשפה עברית; שמות הגיליונות נחתכים ל-31 תווים ומנוקים מתווים אסורים (`: \ / ? * [ ]`).

## סטריפ הסטטיסטיקות וציר ההתקדמות בטאב הפודים

⚠️ **הסטריפ (סה"כ / ממתינים / בתהליך / הושלמו / נכשלו) וציר ההתקדמות מחושבים על `filtered` —
הרשימה שאחרי כל המסננים — ולא על `allPods`.** כך **כל ששת המסננים** משנים את המספרים באותה מידה.

- שלושה מסננים מיושמים בשאילתה (`group_id`, `status`, `casting_approved`) ושלושה בצד לקוח
  (`type_number`, `direction`, `stage`). עד לתיקון הזה הסטריפ קרא את `allPods` — התוצאה של
  השאילתה בלבד — ולכן שלושת המסננים של צד הלקוח לא הפחיתו את "סה"כ פודים" ונראה כאילו המסנן
  לא עבד. **בהוספת מסנן חדש בצד לקוח יש להוסיף אותו לשרשרת `filtered` בלבד** — הסטריפ מתעדכן
  מעצמו.
- הסטריפ מרונדר **לפני** ה-`return` של המצב הריק, כך שסינון בלי תוצאות מציג אפסים ולא מספרים ישנים.
- ציר ההתקדמות: **סך שלבים שהושלמו** מתוך `filtered.length × qcStageSet(...).length`, כולל שלבים
  בתוך פודים שבתהליך. מכנה 0 ⇒ 0%.
- ב-`renderProjectCard` (דשבורד): פוד `completed` = 100%, פוד `in_progress` = 50%.

## נעילת שלבים B–F (בדיקות QC)

שלבים B–F נעולים עד לאישור סעיפים 1–7 ביציקת הרצפה (שלב A). הנעילה מתבטלת אם:
1. `pods.casting_approved === true`, **או**
2. שלב A כבר חתום (`status === 'completed'` או `'failed'`)

הלוגיקה ב-`loadQCStages` ב-`js/qc.js`:
```js
const stageA = _qcStages.find(s => s.stage_number === 1);
if (stageA?.status === 'completed' || stageA?.status === 'failed') {
  _castingBaseApproved = true;
}
```

## מחזור החיים של `pods.casting_approved` (מאושר ליציקה)

הדגל מסמן **"מאושר וממתין ליציקה"** — הפוד ברשימת היציקה, לא "הפוד אושר פעם".
הוא נדלק כשסעיפים 1–7 של שלב A עוברים (`checkCastingApprovalTrigger`) ומכובה כשהפוד כבר יצוק.

**איפוס הדגל — שלושה מסלולים:**
1. אחד מסעיפי הפוסט-יציקה של שלב A (`segregation` / `drainage_test`) נענה — `handleItemStatusChange`.
2. **חתימה על שלב A** — במסלול החתימה ב-`qc.js` (נוסף 23/08/2026). הנעילה של B–F לא נפגעת:
   שלב A חתום משחרר אותם בעצמו (ראו הסעיף הקודם).
3. "ניקוי טופס" של שלב A — `clearStage`.

⚠️ **התג בתצוגה נגזר ולא נקרא ישירות מהעמודה.** `podAwaitingCasting(pod, stages)` ו-
`podPastCasting(stages)` ב-`qc-data.js` הם המקור היחיד: התג מוצג רק אם הדגל דלוק **וגם**
הפוד לא עבר את שער היציקה — כלומר שלב A אינו חתום ואין שלב מאוחר יותר עם עבודה עליו
(`status !== 'pending'`). משמשים בכרטיס הפוד ובמסגרת הירוקה (`renderPodCard`), בכותרת מסך הפוד
(`openPod` + רענון ב-`loadQCStages`), ו**במסנן "מאושרים ליציקה"**.

- **המסנן עבר לצד לקוח** ב-`loadPodsTab` (היה `query.eq('casting_approved', ...)`) — אחרת הרשימה
  הייתה מציגה פודים שהתג שלהם כבוי. בהוספת נגזרת תצוגה חדשה יש להחיל אותה גם על המסנן.
- **למה הפודים נתקעו**: בודק שענה על סעיפים 8–9 *לפני* שסעיפים 1–7 הפעילו את האישור לא חזר
  אליהם, ואחרי חתימת שלב A הסעיפים מוקפאים ע"י `protect_signed_stage_items` — כך שאף מסלול
  לא יכול היה לאפס את הדגל. התוצאה: פודים בשלב E עם תג "מאושר ליציקה".
- **מיגרציה `20260823000000_clear_stale_casting_approved.sql`** מנקה את השורות התקועות ששלב A
  שלהן חתום. **אינה חובה** (התצוגה כבר נכונה בלעדיה) אבל מיישרת את ה-DB. **פודים ששלב A שלהם
  עדיין פתוח אינם נכללות בה בכוונה** — שם הדגל עדיין משחרר את נעילת B–F, ואיפוסו היה נועל מחדש
  פוד שכבר עובדים עליו.
- ⚠️ **אין לחשב `casting_approved` מחדש בצד לקוח כ"כתיבה בקריאה"** (איפוס אוטומטי בפתיחת פוד):
  ל-viewer אין הרשאת UPDATE על `pods`, וכל שינוי מתועד ב-`qc_audit_log` ע"י trigger.

## מפתח CASTING_ITEM_KEYS

רשימת סעיפי שלב A שחייבים לעבור לפני אישור היציקה מוגדרת **פעם אחת** כקבוע גלובלי ב-`js/qc.js`:
```js
const CASTING_ITEM_KEYS = ['length_dims', 'width_dims', 'pipe_slope', 'pipe_fixation',
  'drainage_channel', 'lifting_bolts', 'shower_parallel'];
```
**אין להגדיר אותה שוב בתוך פונקציות** — כל שינוי ייעשה רק בקבוע הזה.

## איפוס סיסמה

זרימת איפוס הסיסמה מוגנת בשתי שכבות ב-`js/app.js`:

1. **URL**: ה-`redirectTo` כולל `?type=recovery`, שמזוהה ב-`window.location.search`.
2. **sessionStorage**: כשמשתמש שולח טופס "שכחתי סיסמה", נשמר `pendingPasswordReset=1`. מנוקה עם טעינת הדף.

הדגל `window._passwordRecoveryMode` מוגדר **לפני** כל event של Supabase, ומונע `INITIAL_SESSION` / `SIGNED_IN` מלפתוח את האפליקציה. לאחר הגדרת סיסמה בהצלחה — הדף נטען מחדש לסשן נקי.

## קבוצות ביצוע — לוגיקה

### שם קבוצה אוטומטי
בעת יצירת קבוצה חדשה: שם מוצע אוטומטי `G${count+1}` (לפי מספר הקבוצות הקיימות). ניתן לערוך לפני שמירה.

### הרכב פודים (`pod_composition`)
- בעת יצירת קבוצה: המשתמש מזין כמות לכל סוג/כיוון (T1-ימין, T1-שמאל, T2-ימין...). הערכים נשמרים כ-`pod_composition jsonb` בטבלת `production_groups`.
- **יצירת פודים אוטומטית**: לאחר שמירת הקבוצה, נוצרים פודים אוטומטית לפי ההרכב. המספרים הסידוריים (`globalSerial`) ממשיכים ברצף מהאחרון הקיים בפרויקט.
- **סדר יצירת הפודים בקבוצה**: לפי `type_number` עולה, ובתוך כל טיפוס — ימין (R) לפני שמאל (L), בהתאמה לסדר ב-`createProject`. **אין למיין לפי מפתחות `comp`** (`typeId_dirId`) — אלה UUID-ים והמיון שלהם אקראי (באג היסטורי: פודי T9 קיבלו מספרים סידוריים לפני T1). הלוגיקה ב-`showGroupModal` ב-`js/projects.js`.
- בעריכת קבוצה: **רק תאריכים ניתנים לעריכה** — שם וה הרכב נעולים. ההרכב מוצג read-only (נגזר מהפודים הממשיים בקבוצה אם `pod_composition` ריק).

### תצוגת כרטיסי קבוצה
- מציג תגיות הרכב (T1 ימין: N) לכל הקבוצות — כולל קבוצות ישנות שאין להן `pod_composition` שמור, על ידי חישוב דינמי מהפודים בפועל.
- מציג תאריך יציקה (`casting_target_date`) ותאריך יעד (`target_date`).

### תצוגת קבוצה בכרטיסי פוד ובפרטי פוד
- מוצגת **נקודה צבעונית + שם הקבוצה בכחול בלבד** — ללא תפריט נגלל.
- אין אפשרות להעביר פוד בין קבוצות לאחר יצירה.

## Audit trail (qc_audit_log)

מיגרציה `20260713000000_qc_audit_log.sql`. Triggers ברמת ה-DB על `qc_stages` ו-`mold_checks` מתעדים מעברי סטטוס מהותיים (חתימה / פתיחה מחדש / ניקוי) עם המשתמש המבצע. **אין ללקוח שום הרשאת כתיבה על הטבלה** (אין פוליסות INSERT/UPDATE/DELETE) — הרשומות חסינות לשינוי ולמחיקה מהאפליקציה. מעברי שגרה `pending→in_progress` לא מתועדים בכוונה (רעש).

**צפייה: כל משתמש פעיל** (מיגרציה `20260726000000_audit_log_view_all_users.sql`, מחליפה את פוליסת האדמין-בלבד מ-20260713000000). הפוליסה היא `current_user_role() IS NOT NULL` — כלומר משתמש שממתין לאישור אדמין או שנוטרל לא רואה כלום, כמו בשאר הטבלאות. **הרשאת הצפייה נשלטת ב-RLS, לא בהסתרת כפתור** — הסרת גייטינג בצד לקוח בלי מיגרציה מתאימה תיתן חלון ריק ("אין רישומי היסטוריה").

**שתי נקודות תצוגה** — כפתור 📜, גלוי לכל המשתמשים:

| מה | איפה | פונקציה | סינון השאילתה |
|---|---|---|---|
| שלבי QC + סעיפים (A–F) | כותרת מסך פוד | `showPodHistory` ב-`pods.js` | `pod_id` |
| בדיקות תבניות | כרטיס הבדיקה בטאב "בדיקות תבניות" | `showMoldHistory` ב-`molds.js` | `table_name='mold_checks'` + `record_id` |

**אישור יציקה מתועד גם הוא**: trigger `log_pod_casting_change` על `pods` כותב `casting_approved` / `casting_removed` בכל שינוי של `pods.casting_approved`. הוא **נוצר ידנית ב-DB מחוץ ל-repo** (SQL Editor או Bolt) והגדרתו נשמרה בדיעבד ב-`20260726010000_pod_casting_audit.sql` — מיגרציית תיעוד, no-op על ה-DB החי, כמו `20260712050000_mold_checks_table.sql`. הרשומות נכתבות עם `pod_id = NEW.id` ולכן מוצגות בהיסטוריית הפוד (`showPodHistory` לא מסננת `table_name`). התוויות: `audit.casting_approved` / `audit.casting_removed`.

⚠️ **רשומות `mold_checks` נכתבות עם `pod_id = NULL`** (בדיקת תבנית לא משויכת לפוד) — לכן `showMoldHistory` מסננת לפי `record_id` ולא לפי `pod_id`. אין לאחד את שתי הפונקציות לשאילתה אחת לפי `pod_id`.

## PWA / Service Worker

- `manifest.json` + `sw.js` + רישום ב-`app.js`. אסטרטגיה: קריאות Supabase/HIBP לעולם לא נשמרות ב-cache; ניווטים network-first עם fallback ל-shell; נכסים סטטיים (מקומיים + jsdelivr) stale-while-revalidate.
- **לאחר deploy ייתכן שרענון ראשון יגיש גרסה קודמת של נכס סטטי** (הרענון מתבצע ברקע) — רענון שני מקבל את החדש. מספרי `?v=` עדיין חובה.
- אייקונים: `images/icon-192.png` / `icon-512.png` (נוצרו מ-apple-touch-icon).

## שמות קבצים בהעלאה ל-Storage

מפתחות ב-Supabase Storage חייבים להיות ASCII. שם קובץ בעברית (למשל
`02-בילינסון-B100-פריסות.pdf`) נדחה עם `Invalid key` ומפיל את ההעלאה.

**`safeStorageName(name, fallbackExt)`** ב-`config.js` מנקה את השם עבור **נתיב האחסון בלבד**:
תווים לא-ASCII → `_`, סיומת מנורמלת, קיצור ל-60 תווים, ונפילה ל-`file.<ext>` אם לא נשאר כלום.
משמש ב-`uploadPlan` (`projects.js`) וב-`uploadQcImage` (`qc.js`).

⚠️ **השם המקורי נשמר כמות שהוא** ב-`type_plans.file_name` ומוצג למשתמש — אין להחליף אותו
בשם המנוקה, אחרת המשתמש יאבד את היכולת לזהות את הקובץ שהעלה.

## דחיסת תמונות QC

`compressImage` ב-`qc.js` מקטין תמונות לפני העלאה (מקס' 1600px, JPEG q0.8, מדלג על קבצים מתחת ל-400KB). כשל בדחיסה ⇒ העלאת המקור.

## בדיקות תבניות (mold_checks)

פיצ'ר שהוחזר לשימוש (נוצר במקור ב-Bolt; הקוד המקורי לא היה ב-repo). מודול `js/molds.js`, טאב "בדיקות תבניות" במסך פרויקט.

- **מבנה**: בדיקה לפי `type_id` (טיפוס בפרויקט) + `direction` (R/L/ללא) + `mold_number`. 9 סעיפים בעמודות שטוחות `<key>_status/_notes` (+`_value` לאורך/רוחב) — מוגדרים ב-`MOLD_CHECK_ITEMS` ב-`molds.js`; תוויות ב-i18n תחת `mold.item.*`.
- **שאילתות בלי FK embedding**: הטבלה ב-DB החי נוצרה ב-Bolt וייתכן שאין בה constraint מוצהר — לכן `loadMoldsTab` שולף types ואז `mold_checks` עם `.in('type_id', ...)`, לא join של PostgREST.
- **הרשאות**: כתיבה admin+inspector (מיגרציה 20260712040000), כפתור ההוספה `.inspector-only`. מחיקה ב-UI לאדמין בלבד; כפתור ההיסטוריה 📜 גלוי לכל המשתמשים (`showMoldHistory` — ראו "Audit trail").
- **שמירה**: הטופס נשמר בלחיצת "שמור" (לא autosave); "חתום וסיים" עובר לשלב חתימה בתוך אותו מודאל (SignaturePad נפרד — `_moldSigPad`), וכישלון בסעיף כלשהו ⇒ `status='failed'`.
- הגדרת הטבלה מתועדת ב-`20260712050000_mold_checks_table.sql` (no-op על ה-DB החי).

## שלמות נתוני QC — הגנות מאז 20260719 (תקלת 29/03)

רקע: ב-29/03/2026 נוקו טופסי שלב A של כמה פודים ("ניקוי טופס"), מולאו מחדש חלקית (בלי סעיפי המדידה 1, 2, 7) ונחתמו "הושלם" — בלי התרעה ובלי תיעוד. ההגנות שנוספו:

- **ולידציית שלמות בחתימה** (`btn-complete-stage` ב-`qc.js`): חתימה על שלב עם סעיפים לא מסומנים פותחת `uiConfirm` עם רשימת הסעיפים החסרים (`qc.signMissingItems`). אפשר לאשר ולחתום בכל זאת — אבל לא בשוגג.
- **מיגרציה `20260719000000_protect_qc_items.sql`** (להריץ ידנית ב-SQL Editor):
  - trigger `protect_signed_stage_items` חוסם INSERT/UPDATE/DELETE על `qc_items` כשהשלב ההורה `completed`/`failed` — סוגר גם טאבים ישנים שנשארו פתוחים. **לכן `clearStage` מאפס את השלב לפני הסעיפים** — אין להחזיר את הסדר הישן (items→stage) שייחסם ע"י ה-trigger.
  - trigger `log_qc_item_change` מתעד ב-`qc_audit_log` כל שינוי סימון קיים (`passed`/`failed` → אחר) כ-`item_status_changed`; מוצג בהיסטוריית הפוד (📜) עם שם הסעיף והמעבר. סימון ראשוני לא מתועד (רעש).
- **בדיקת שגיאות בשמירת סימון** (`handleItemStatusChange`): כישלון כתיבה מחזיר את השורה למצב האחרון שנשמר ומציג `qc.itemSaveError` — לא נשאר ✓ כוזב על המסך.
- **"ניקוי טופס" לאדמין בלבד** ב-UI (היה לכל `canEdit`).
- **מכנה מונה הדוח ב-PDF** (`reports.js`): `passed/totalDefined` לפי הגדרת השלב — לא לפי מספר שורות ה-DB (שגרם לטופס חסר להיראות "6/6 עברו").

## תיעוד תאריכי בדיקה (מיגרציה 20260719020000)

- **`pods.inspection_started_at`** — נקבע ב-trigger ברגע הסימון הראשון (passed/failed) של סעיף כלשהו בפוד, פעם אחת. מוצג ב-info-bar של מסך הפוד ("תחילת בדיקה"). Backfill לפודים קיימים לפי `MIN(created_at)` של סעיפיהם.
- **`qc_items.fixed_at`** — נקבע ב-trigger במעבר `failed`→`passed` (תיקון); מתאפס אם הסעיף חוזר ל-`failed`/`pending`. מוצג בשורת הסעיף (🔧 "תוקן בתאריך") ובדוח ה-PDF.
- שני התאריכים נכתבים רק ב-DB (triggers, SECURITY DEFINER) — אין כתיבה מהלקוח.

## שינויים שבוצעו

### היסטוריית שינויים ודוחות חלון-זמן (26/07/2026)

חמש מסירות ברצף (PR #31–#35). הפירוט המלא נמצא בסעיפים **"Audit trail (qc_audit_log)"** ו-**"מסך הדוחות — שלושה טאבים"**; כאן רק מה שלא מכוסה שם ומה שצריך לדעת בקצרה.

| # | מה | קבצים |
|---|---|---|
| 31 | תצוגת היסטוריה לבדיקות תבניות (`showMoldHistory`) + פתיחת הצפייה לכל משתמש פעיל | `molds.js`, `index.html`, מיגרציה `20260726000000` |
| 32 | כפתור ההיסטוריה במסך פוד קיבל טקסט וסגנון של כפתור ההערות | `index.html`, `i18n.js` |
| 33 | תוויות לפעולות אישור יציקה + שמירת ה-trigger כמיגרציה | `i18n.js`, מיגרציה `20260726010000` |
| 34 | שני טאבים חדשים בדוחות: התקדמות עבודה, היסטוריית עריכות | `reports.js`, `i18n.js` |
| 35 | ייצוא Excel לשני הטאבים החדשים | `reports.js`, `i18n.js` |

**כפתור ההיסטוריה במסך פוד** (`#btn-pod-history`): היה אייקון 📜 בלבד ב-`btn-ghost` עם `admin-only`. כיום `btn-secondary` עם `data-i18n="pod.history"` (`📜 היסטוריה` / `📜 History`), בלי גייטינג תפקידים — באותה תבנית של `pod.comments`/`pod.barcode`/`pod.exportPdf`. ה-`aria-label` הוסר כי הטקסט הגלוי ממלא את תפקידו; ה-`title` נשאר כטולטיפ מפורט.

⚠️ **שתי מיגרציות דורשות הרצה ידנית שונה זו מזו:**
- `20260726000000_audit_log_view_all_users.sql` — **חובה להריץ**. בלעדיה כפתור 📜 גלוי לכולם אבל פותח חלון ריק לכל מי שאינו אדמין (ה-RLS מחזיר אפס שורות).
- `20260726010000_pod_casting_audit.sql` — **no-op, אין צורך להריץ**. מיגרציית תיעוד בלבד ל-trigger שכבר קיים ב-DB.

**מה שאין ואי אפשר להוסיף**: היסטוריית עריכות לתקופה שלפני הפעלת ה-audit trail. ה-DB שומר שורה אחת לכל שלב/סעיף והיא נדרסת בכל שינוי — ניקוי טופס, פתיחה מחדש ושינוי סימון פשוט לא השאירו עקבות. **טאב "התקדמות עבודה" לעומת זאת אינו מוגבל** כי הוא נשען על `completed_at`/`created_at` שקיימים לכל ההיסטוריה. אין לנסות "לשחזר" אירועים היסטוריים מ-`completed_at` לטאב ההיסטוריה: `completed_at` שומר רק את החתימה **האחרונה**, כך שטבלה כזו תיראה שלמה בעודה מחביאה בדיוק את הפעולות ההרסניות.

### עריכת אימייל במסך ניהול משתמשים (`js/app.js`, `js/i18n.js`)

בכל שורת משתמש ב-`loadUsersView` נוסף כפתור **"ערוך אימייל"** (`.btn-edit-email`) שפותח מודל (`openModal`) לעריכת השדה.

- **מעדכן רק את `public.profiles.email`** — עמודת תצוגה, כפוף לאותו trigger `protect_profile_privileges` (אדמין פעיל בלבד) כמו שינוי role/is_active/username.
- **אינו משנה את אימייל ההתחברות** ב-`auth.users` (Supabase Auth) — אין ל-client גישת Admin API לכך. עדכון כתובת ההתחברות בפועל נעשה **ידנית ב-Supabase Dashboard או SQL Editor**:
  ```sql
  UPDATE auth.users SET email = '...', email_confirmed_at = now() WHERE id = '<uid>';
  UPDATE auth.identities SET identity_data = jsonb_set(identity_data, '{email}', '"..."') WHERE user_id = '<uid>';
  UPDATE public.profiles SET email = '...' WHERE id = '<uid>';
  ```
- מפתחות i18n חדשים תחת `users.editEmail*` / `users.emailUpdated` / `users.emailUpdateError` / `users.invalidEmail` (`he`+`en`).
- ולידציה בסיסית של פורמט אימייל בצד לקוח לפני השליחה.

### שלב F — התקנת אביזרי קצה (`js/qc-data.js`)

נוספו הסעיפים הבאים לרשימת הבדיקות:
- **מראה** (`key: 'mirror'`) — סעיף 18
- **סיפון כיור** (`key: 'sink_siphon'`) — סעיף 19
- **סיפון מזגן** (`key: 'ac_siphon'`, `AC condensate siphon`) — סעיף 20 (אחרון)

בנוסף, הסעיף הקיים `shower` שונה מ"מקלחון" ל**"מקלחון זכוכית"** (`Shower glass`) — **שינוי תצוגה בלבד**: ה-`key` נשאר `shower`, ולכן כל רשומות ה-`qc_items` הקיימות (כולל בפודים חתומים) נשמרות ומוצגות תחת התווית החדשה. אין להחליף `key` של סעיף קיים כדי לשנות תווית — זה מנתק את הרשומות ההיסטוריות.

**פודים ששלב F כבר נחתם בהם**: `ac_siphon` הוא הסעיף החדש היחיד, ולכן יופיע בהם כלא-מסומן (הדוח יראה `18/20` לשלב F). זה **מכוון ותקין** — הסעיף לא היה קיים בעת החתימה, וזה תיעוד נאמן של מה שנבדק בפועל. **אין לסמן אותו אוטומטית בכמות** (זיוף נתוני בדיקה — בדיוק מה שההגנות מ-20260719 מונעות). השלב החתום נשאר `completed` (ה-trigger `protect_signed_stage_items` חוסם הזרקת הסעיף, ו-`updateStageStatus` לא נוגע בשלבים חתומים). עדכון פוד ספציפי ייעשה רק דרך "עריכת טופס" → בדיקה → חתימה מחדש (מבוקר ומתועד).

### ברקוד (`js/pods.js`, `js/projects.js`)
- גודל שם הפוד בברקוד: 18pt (היה 11pt).
- פריסה: ברקוד בשורה עליונה, שם הקבוצה (G1/G7...) + קוד הפוד בשורה תחתונה זו לצד זו.
- שם הקבוצה נשלף ישירות מ-`pod.production_groups?.name` — ללא חישוב אותיות A/B/C.
- הקבוצות נטענות לכל המשתמשים (לא רק Admin/PM) כדי שהברקוד יכלול שם קבוצה.
- **סדר ברקודים בקבוצה חדשה**: תוקן באג שבו פודים נוצרו לפי מיון מחרוזתי של UUID (T9 קיבל מספרים סידוריים לפני T1). כיום היצירה לפי `type_number` וכיוון — ראו "סדר יצירת הפודים בקבוצה" בסעיף קבוצות ביצוע. חל על קבוצות חדשות בלבד; מספור קיים לא שונה.

#### מידות הדפסה — חובה לשמור על סנכרון בין שני המסלולים
שני מסלולי ההדפסה חייבים להישאר **זהים** ב-CSS ההדפסה: `showBarcodeModal` (`js/pods.js`, פוד בודד) ו-`printAllBarcodes` (`js/projects.js`, כל הפודים). כל שינוי במידות/פריסה חייב להתבצע בשניהם.

- **אזור ההדפסה: 145×57.2 מ"מ** (רוחב 14.5 ס"מ; הגובה נגזר מהפרופורציה ההיסטורית 142×56 → 57.2). ממורכז בעמוד **155×67.2 מ"מ** עם `@page{size:155mm 67.2mm;margin:0}` — כך שנשארים **5 מ"מ (0.5 ס"מ) שוליים זהים בכל ארבעת הצדדים**.
- **הפריסה אופקית, ללא סיבוב 90°.** בעבר העמוד היה 62×150 מ"מ (מדבקת Brother QL-700) והתוכן סובב `rotate(-90deg)`; זה הוסר. **גודל העמוד הנוכחי אינו תואם לסרט 62 מ"מ** — יש לוודא שהמדפסת/המדיה מוגדרת ל-155×67.2 מ"מ.
- **הברקוד נמתח למילוי מלא של גובה אזור ההדפסה** כדי שהשוליים העליון/תחתון יהיו זהים לצדדים (אחרת נשאר אוויר לבן עודף למעלה/למטה, כי הברקוד נמוך מהקופסה). המימוש: `preserveAspectRatio="none"` על ה-SVG (ל-JsBarcode יש `viewBox`, לכן המתיחה עובדת) + `.bw{flex:1}` ו-`.bw svg{height:100%}`. מתיחה **אנכית** אינה פוגעת בקריאות CODE128 — יחסי רוחב הפסים נשמרים; רק גובה הפסים גדל. **אין להחזיר `max-height` על ה-SVG** — הוא מחזיר את האוויר הלבן.

### באנר הערות (`js/qc.js`)
- תוקן ניסוח: "הערות פתוחות" (היה "הערהות פתוחהות").

### Bolt sync ⚠️
הפרויקט מחובר לשני כלים שדוחפים ל-GitHub: **קלוד** (commits חתומים `Claude`) ו-**Bolt** (commits חתומים `dlkenig-lang`, כלומר בשם המשתמש).

- **סיכון דריסה**: ל-Bolt עותק משלו של הקוד. אם Bolt מחזיק גרסה ישנה ומסנכרן, ה-commit שלו **דורס את `main`** ומבטל עבודה חדשה — גם בלי commit ידני. כך נמחק פעם אחת כל ה-i18n (commit `195325f`), ושוחזר ב-revert (`d696cde`).
- **לפני עבודה ב-Bolt**: לוודא שהוא מציג את הגרסה העדכנית. אם לא — לרענן/לעבור לענף אחר וחזרה ל-`main`, או `Disconnect project`.
- **סימני זיהוי לגרסה עדכנית** (יש לעדכן את השורה הזו בכל פיצ'ר גלוי חדש, אחרת הבדיקה הופכת חסרת תועלת):
  - **שדה "שם דגם"** לכל טיפוס במודל יצירת פרויקט ובטאב "פרטים נוספים"
  - **שדה "מספר פרויקט"** במודל יצירת פרויקט חדש (נכנס לקוד היחידה כ-`PPMMYY`)
  - **שדה "סוג מוצר"** (פוד סניטרי / פאנל רפואי) במודל יצירת פרויקט חדש
  - מתג עב/EN ב-navbar (מ-i18n)
  - כפתור **📜 היסטוריה** עם טקסט בכותרת מסך פוד — לא אייקון בלבד
  - **שלושה טאבים** במסך דוחות: דוח QC / התקדמות עבודה / היסטוריית עריכות
  - **שלב F** (התקנת אביזרי קצה): סעיף **"סיפון מזגן"** קיים, והסעיף הראשי נקרא **"מקלחון זכוכית"** (לא "מקלחון")
  - **מסנן שלב** ("כל השלבים") בשורת המסננים של טאב הפודים
  - **טאב "יעדי משלוח"** במסך הפרויקט + כפתור **🚚 תעודת משלוח** במסך הפוד
  - **טבלת "יעד ייצור"** בטאב הפודים (כשמוגדרות כמויות יעד, עם כפתור צמצום ▾) + כפתור **✖ סמן כנגרע** במסך הפוד
- אם Bolt לא מציג שינויים: עבור לענף אחר וחזור ל-main בממשק Bolt.
- **שחזור לאחר דריסה**: העבודה לא אובדת — היא בהיסטוריה. `git revert <commit-הדורס>` מחזיר את המצב (קדימה, ללא force-push).
