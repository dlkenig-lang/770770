-- =============================================
-- Human-readable model name per project type
-- =============================================
-- ⚠️ יש להריץ ידנית ב-Supabase SQL Editor.
--
-- הרקע: הבודק רואה "T1" ואין לו דרך לדעת איזה דגם זה. המידות לא פותרות —
-- בפרויקט בילינסון ל-T1 (בסיסי) ול-T3 (מוגבר) יש בדיוק אותן מידות 120x244,
-- כך שרק שם מפורש מבדיל ביניהם.
--
-- השדה חופשי ואופציונלי. כשהוא ריק מוצג "T1" בלבד, כלומר כל הפרויקטים
-- הקיימים ממשיכים להיראות בדיוק כמו קודם.

ALTER TABLE project_types
  ADD COLUMN IF NOT EXISTS model_name TEXT;
